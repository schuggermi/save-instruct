<div class="card p-2 d-flex flex-row justify-content-between align-items-center">
  <%# TODO Implement how many employees assigned and how many steps %>

  <div class="flex-grow-1">
    <h3 class="fw-bold"><%= task.name %></h3>
    <p><%= task.description %></p>
  </div>

  <div>
    <%= link_to 'Edit task', edit_task_path(task), class: "btn btn-info" %>
    <%= link_to 'More details', task_path(task), class: "btn btn-info" %>
  </div>
</div>
