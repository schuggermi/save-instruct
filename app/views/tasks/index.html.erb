<div class="container-fluid">
  <% if current_user.admin? %>
    <div>
      <h1 class="text-center my-4">Tasks</h1>
    </div>
    <% @tasks.each do |task| %>
      <%= render "task-card", task: task %>
    <% end %>
  <% else %>
    <%# <% if current_user.visits.count == 1 %>
      <%# <div class="my-5"> %>
        <%# <%= render :template => 'devise/passwords/edit', locals: { resource: User.find(current_user.id), resource_name: "user" } %>
      <%# </div> %>
    <%# <% else %>
      <div>
        <h1 class="text-center my-4">Tasks</h1>
      </div>
      <% @employee_tasks.each do |e_task| %>
        <%= render "task-card", task: e_task.task %>
      <% end %>
    <%# <% end %>
  <% end %>
</div>
