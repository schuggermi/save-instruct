<%# <% @employees.each do |employee| %>
<%# <% end %>

<%= simple_form_for [@task, @employee_task] do |f| %>
  <% @employees.each do |employee| %>
    <%# <%= f.label :user, input_html: { value: employee.first_name } %>
    <%= f.input :user, as: :check_boxes, collection: @employees %>
  <% end %>
  <%= f.submit %>
<% end %>
