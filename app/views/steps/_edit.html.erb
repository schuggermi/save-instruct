<%= simple_form_for [task, step], html: { class:"d-none", data: { "edit-step-target": "form", action: "submit->edit-step#update" } } do |f| %>
  <%= f.input :instruction %>
  <%= f.input :info %>
  <%= f.submit %>
<% end %>
